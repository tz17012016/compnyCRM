import { ParagraphCaptialPipe } from './paragraph-captial.pipe';

describe('ParagraphCaptialPipe', () => {
  it('create an instance', () => {
    const pipe = new ParagraphCaptialPipe();
    expect(pipe).toBeTruthy();
  });
});
